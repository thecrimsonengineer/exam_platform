name: Phase L One Shot Formatter

on:
  push:
    branches:
      - phase-l-lab-engine

permissions:
  contents: write

concurrency:
  group: phase-l-format-once
  cancel-in-progress: true

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.9'
          channel: stable
          cache: true

      - name: Format Phase L Dart
        run: |
          dart format \
            lib/features/lab \
            lib/screens/lab \
            lib/screens/admin/lab \
            test/features/lab

      - name: Commit canonical formatting
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet; then
            echo "Phase L is already formatted."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add lib/features/lab lib/screens/lab lib/screens/admin/lab test/features/lab
          git commit -m "Apply canonical Dart format for Phase L2"
          git push origin HEAD:phase-l-lab-engine
